// Source code is decompiled from a .class file using FernFlower decompiler (from Intellij IDEA).
package controller;

import dao.UserDAO;
import java.awt.Component;
import java.awt.HeadlessException;
import java.io.File;
import java.io.IOException;
import java.net.URL;
import java.util.ResourceBundle;
import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.fxml.FXMLLoader;
import javafx.fxml.Initializable;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.control.PasswordField;
import javafx.scene.control.TextField;
import javafx.scene.input.MouseEvent;
import javafx.scene.media.Media;
import javafx.scene.media.MediaPlayer;
import javafx.stage.Stage;
import javax.swing.JOptionPane;
import model.User;

public class LoginController implements Initializable {
   public static User user;
   @FXML
   private TextField usernameField;
   @FXML
   private PasswordField passwordField;
   @FXML
   private Button loginButton;
   @FXML
   private Button signUpButton;
   private MediaPlayer mediaPlayer;

   public LoginController() {
   }

   public void initialize(URL url, ResourceBundle rb) {
      user = null;
      this.loginButton.setText("LOGIN");
      this.playBackgroundMusic();
   }

   @FXML
   private void handleButtonEvent(ActionEvent event) throws IOException {
      this.stopBackgroundMusic();
      if (!this.usernameField.getText().isEmpty() && !this.passwordField.getText().isEmpty()) {
         if (this.loginButton.getText().equals("LOGIN")) {
            try {
               user = UserDAO.validate(this.usernameField.getText(), this.passwordField.getText());
               if (user != null) {
                  Stage stage = (Stage)this.loginButton.getScene().getWindow();
                  URL url = (new File("src/main/java/view/MainMenu.fxml")).toURI().toURL();
                  Parent root = (Parent)FXMLLoader.load(url);
                  Scene scene = new Scene(root);
                  stage.setScene(scene);
               } else {
                  JOptionPane.showMessageDialog((Component)null, "INVALID USERNAME/PASSWORD!!!");
               }
            } catch (IOException | HeadlessException var7) {
               var7.printStackTrace();
            }
         } else {
            User u = new User(this.usernameField.getText(), this.passwordField.getText(), "password");
            UserDAO.registerUser(u);
            JOptionPane.showMessageDialog((Component)null, " Registered " + this.usernameField.getText() + " Successfully. Please Login!");
            Stage stage = (Stage)this.loginButton.getScene().getWindow();
            URL url = (new File("src/main/java/view/MainMenu.fxml")).toURI().toURL();
            Parent root = (Parent)FXMLLoader.load(url);
            Scene scene = new Scene(root);
            stage.setScene(scene);
         }

      } else {
         JOptionPane.showMessageDialog((Component)null, "Username dan Password tidak boleh kosong!");
      }
   }

   @FXML
   void handleSignUpButtonEvent(MouseEvent event) throws IOException {
      this.stopBackgroundMusic();

      try {
         URL url = (new File("src/main/java/view/Signup.fxml")).toURI().toURL();
         Parent root = (Parent)FXMLLoader.load(url);
         Stage stage = (Stage)this.signUpButton.getScene().getWindow();
         Scene scene = new Scene(root);
         stage.setScene(scene);
         stage.show();
      } catch (IOException var6) {
         var6.printStackTrace();
         JOptionPane.showMessageDialog((Component)null, "Gagal memuat halaman Signup. Periksa file FXML Anda!");
      }

   }

   private void playBackgroundMusic() {
      String musicFile = "src/main/java/assets/login.mp3";
      File musicPath = new File(musicFile);
      if (musicPath.exists()) {
         Media media = new Media(musicPath.toURI().toString());
         this.mediaPlayer = new MediaPlayer(media);
         this.mediaPlayer.setCycleCount(-1);
         this.mediaPlayer.play();
      } else {
         JOptionPane.showMessageDialog((Component)null, "Musik tidak ditemukan!");
      }

   }

   private void stopBackgroundMusic() {
      if (this.mediaPlayer != null) {
         this.mediaPlayer.stop();
      }

   }
}

