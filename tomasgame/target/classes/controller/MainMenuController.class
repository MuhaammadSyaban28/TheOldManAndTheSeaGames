// Source code is decompiled from a .class file using FernFlower decompiler (from Intellij IDEA).
package controller;

import java.awt.Component;
import java.io.File;
import java.io.IOException;
import java.net.URL;
import javafx.fxml.FXML;
import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.input.MouseEvent;
import javafx.scene.media.Media;
import javafx.scene.media.MediaPlayer;
import javafx.stage.Stage;
import javax.swing.JOptionPane;

public class MainMenuController {
   @FXML
   private Button startGameButton;
   @FXML
   private Button bookButton;
   @FXML
   private Button historyButton;
   @FXML
   private Button leaderboardButton;
   @FXML
   private Button quitButton;
   private MediaPlayer mediaPlayer;

   public MainMenuController() {
   }

   public void initialize() {
      this.playBackgroundMusic();
   }

   @FXML
   private void handleHistory(MouseEvent event) {
      this.stopBackgroundMusic();

      try {
         URL url = (new File("src/main/java/view/History.fxml")).toURI().toURL();
         Parent root = (Parent)FXMLLoader.load(url);
         Stage stage = (Stage)this.historyButton.getScene().getWindow();
         Scene scene = new Scene(root);
         stage.setScene(scene);
         stage.show();
      } catch (IOException var6) {
         var6.printStackTrace();
         JOptionPane.showMessageDialog((Component)null, "Gagal memuat halaman history. Periksa file FXML Anda!");
      }

   }

   @FXML
   void handleLeaderboard(MouseEvent event) throws IOException {
      this.stopBackgroundMusic();

      try {
         URL url = (new File("src/main/java/view/Leaderboard.fxml")).toURI().toURL();
         Parent root = (Parent)FXMLLoader.load(url);
         Stage stage = (Stage)this.leaderboardButton.getScene().getWindow();
         Scene scene = new Scene(root);
         stage.setScene(scene);
         stage.show();
      } catch (IOException var6) {
         var6.printStackTrace();
         JOptionPane.showMessageDialog((Component)null, "Gagal memuat halaman Login. Periksa file FXML Anda!");
      }

   }

   @FXML
   void handleQuitButtonEvent(MouseEvent event) throws IOException {
      this.stopBackgroundMusic();

      try {
         URL url = (new File("src/main/java/view/Login.fxml")).toURI().toURL();
         Parent root = (Parent)FXMLLoader.load(url);
         Stage stage = (Stage)this.quitButton.getScene().getWindow();
         Scene scene = new Scene(root);
         stage.setScene(scene);
         stage.show();
      } catch (IOException var6) {
         var6.printStackTrace();
         JOptionPane.showMessageDialog((Component)null, "Gagal memuat halaman Leaderboard. Periksa file FXML Anda!");
      }

   }

   @FXML
   private void handleGame(MouseEvent event) {
      this.stopBackgroundMusic();

      try {
         URL url = (new File("src/main/java/view/Game.fxml")).toURI().toURL();
         Parent root = (Parent)FXMLLoader.load(url);
         Stage stage = (Stage)this.startGameButton.getScene().getWindow();
         Scene scene = new Scene(root);
         stage.setScene(scene);
         stage.show();
      } catch (IOException var6) {
         var6.printStackTrace();
         JOptionPane.showMessageDialog((Component)null, "Gagal memuat halaman game. Periksa file FXML Anda!");
      }

   }

   @FXML
   private void handleBook(MouseEvent event) {
      this.stopBackgroundMusic();

      try {
         URL url = (new File("src/main/java/view/Book.fxml")).toURI().toURL();
         Parent root = (Parent)FXMLLoader.load(url);
         Stage stage = (Stage)this.bookButton.getScene().getWindow();
         Scene scene = new Scene(root);
         stage.setScene(scene);
         stage.show();
      } catch (IOException var6) {
         var6.printStackTrace();
         JOptionPane.showMessageDialog((Component)null, "Gagal memuat halaman buku. Periksa file FXML Anda!");
      }

   }

   private void playBackgroundMusic() {
      String musicFile = "src/main/java/assets/mainmenu.mp3";
      File musicPath = new File(musicFile);
      if (musicPath.exists()) {
         Media media = new Media(musicPath.toURI().toString());
         this.mediaPlayer = new MediaPlayer(media);
         this.mediaPlayer.setCycleCount(-1);
         this.mediaPlayer.play();
      } else {
         JOptionPane.showMessageDialog((Component)null, "Musik tidak ditemukan!");
      }

   }

   private void stopBackgroundMusic() {
      if (this.mediaPlayer != null) {
         this.mediaPlayer.stop();
      }

   }
}

