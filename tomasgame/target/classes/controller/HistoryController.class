// Source code is decompiled from a .class file using FernFlower decompiler (from Intellij IDEA).
package controller;

import dao.HistoryDAO;
import java.awt.Component;
import java.io.File;
import java.io.IOException;
import java.net.URL;
import java.util.List;
import javafx.collections.FXCollections;
import javafx.collections.ObservableList;
import javafx.fxml.FXML;
import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.control.TableColumn;
import javafx.scene.control.TableView;
import javafx.scene.control.cell.PropertyValueFactory;
import javafx.scene.input.MouseEvent;
import javafx.scene.media.Media;
import javafx.scene.media.MediaPlayer;
import javafx.stage.Stage;
import javax.swing.JOptionPane;
import model.History;
import model.User;

public class HistoryController {
   @FXML
   private Button backBtn;
   @FXML
   private TableView<History> historyTable;
   @FXML
   private TableColumn<History, Integer> colIdHistory;
   @FXML
   private TableColumn<History, String> colLevel;
   @FXML
   private TableColumn<History, String> colDate;
   @FXML
   private TableColumn<History, Integer> colScore;
   @FXML
   private TableColumn<History, String> colUserName;
   private ObservableList<History> historyList;
   private MediaPlayer mediaPlayer;

   public HistoryController() {
   }

   @FXML
   public void initialize() {
      this.colIdHistory.setCellValueFactory(new PropertyValueFactory("idHistory"));
      this.colLevel.setCellValueFactory(new PropertyValueFactory("level"));
      this.colDate.setCellValueFactory(new PropertyValueFactory("date"));
      this.colScore.setCellValueFactory(new PropertyValueFactory("score"));
      this.colUserName.setCellValueFactory(new PropertyValueFactory("username"));
      this.historyList = FXCollections.observableArrayList();
      this.loadHistoryData();
      this.historyTable.setItems(this.historyList);
      this.playBackgroundMusic();
   }

   private void loadHistoryData() {
      User currentUser = LoginController.user;
      if (currentUser != null) {
         int userId = currentUser.getUid();
         List<History> histories = HistoryDAO.getHistoryById(userId);
         this.historyList.addAll(histories);
      }

   }

   @FXML
   private void HistoryBack(MouseEvent event) {
      try {
         this.stopBackgroundMusic();
         URL url = (new File("src/main/java/view/MainMenu.fxml")).toURI().toURL();
         Parent root = (Parent)FXMLLoader.load(url);
         Stage stage = (Stage)this.backBtn.getScene().getWindow();
         Scene scene = new Scene(root);
         stage.setScene(scene);
         stage.show();
      } catch (IOException var6) {
         var6.printStackTrace();
         JOptionPane.showMessageDialog((Component)null, "Failed to load Main Menu. Please check your FXML file!");
      }

   }

   private void playBackgroundMusic() {
      String musicFile = "src/main/java/assets/history.mp3";
      File musicPath = new File(musicFile);
      if (musicPath.exists()) {
         Media media = new Media(musicPath.toURI().toString());
         this.mediaPlayer = new MediaPlayer(media);
         this.mediaPlayer.setCycleCount(-1);
         this.mediaPlayer.play();
      } else {
         JOptionPane.showMessageDialog((Component)null, "Musik tidak ditemukan!");
      }

   }

   private void stopBackgroundMusic() {
      if (this.mediaPlayer != null) {
         this.mediaPlayer.stop();
      }

   }
}

